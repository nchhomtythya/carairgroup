  <%= simple_form_for([car, review], data: { insert_in_list_target: "form",
          action: "submit->insert-in-list#send"
          }) do |f| %>
    <%= f.input :content, as: :text %>
    <%= f.submit "Submit review", class: "btn btn-primary" %>
  <% end %>
